<%- include('../includes/head.ejs') %>

    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/liked_song.css">
</head>
<body>
    <header>
    <%- include('../includes/menu_side.ejs') %>
        <div class="song_side">
            <%- include('../includes/nav.ejs') %>
            

            <div class="head-section">
                <img src="images\liked_song.jpeg" alt="image">
                <p class="title">Liked Songs</p>
            </div>
            <!-- Liked songs -->
            <% 
            const {
                items: savedItems,   
            } = locals.userSavedTracks %>
            <% if (savedItems.length > 0) { %>
            <div class="liked_songs">
                <ul>
                    <% savedItems.forEach(({track: {album: {images, name}, artists}}) => { %>
                        <li>
                            <img src="<%= images[0].url %>" alt="song_image">
                            <i class="bi bi-play-circle-fill"></i>
                            <% const text = artists.map(({name})=> name).join(', '); %>
                            <h5><%= name %> <br>
                                <div class="artist_name"><%= text %></div>
                            </h5>    
                        </li>
                    <% }); %>
                </ul>
            </div>
            <% } %>
            </div>
        
    <%- include('../includes/master_side.ejs') %>
        
    </header>
</body>
</html>           


